
#include "qore-qt-static-qwidget-methods.h"

#if 0
class T {
#endif

   public:
      DLLLOCAL virtual void accept() { qobj->accept(); }
      DLLLOCAL virtual void done(int r) { /*qobj->done(r);*/ }
      DLLLOCAL virtual void reject() { qobj->reject(); }

#if 0
};
#endif

